{
  "name": "tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:open:noauth": "cypress open --env fgrep='no-auth'",
    "cypress:open:jwt": "cypress open --env fgrep='jwt'",
    "cypress:run": "cypress run",
    "cypress:run:noauth": "NO_COLOR=1 cypress run --env fgrep='no-auth'",
    "cypress:run:jwt": "NO_COLOR=1 cypress run --env fgrep='jwt'",
    "cypress:run:all": "NO_COLOR=1 ./run_all_tests.sh"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^3.3.1",
    "cypress-select-tests": "^1.3.0"
  }
}

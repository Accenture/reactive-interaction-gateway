{
  "name": "tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:open:noauth": "cypress open --env grep='No auth'",
    "cypress:open:jwt": "cypress open --env grep='JWT'",
    "cypress:run": "cypress run",
    "cypress:run:noauth": "cypress run --env grep='No auth'",
    "cypress:run:jwt": "cypress run --env grep='JWT'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^3.3.1",
    "cypress-select-tests": "^1.3.0"
  }
}
